document.getElementById("log").addEventListener("click", function(event){
 event.preventDefault(); // impede o comportamento padrão do botão (enviar o formulário)
 window.location.href = "nova_pagina.html"; // redireciona para a nova página
});